// navigation.js

function navigateTo(page) {
    window.location.href = page; // Weiterleitung zur gewählten Seite
}

function goBack() {
    window.history.back(); // Gehe zur vorherigen Seite zurück
}

